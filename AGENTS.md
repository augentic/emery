# Emery - Agent Instructions

> The v1 implementation (survey + extract, plan/refine/execute, target adapters, the definition loop) is archived at git tag `v1`; retrieve it with `git worktree add ../emery-v1 v1`. First-party adapters live in [`augentic/emery-adapters`](https://github.com/augentic/emery-adapters) (its `v1` tag keeps the survey-era tree).

## What this repository is

A Rust workspace at the repository root producing the `emery` runtime binary, plus one Cursor plugin (`plugins/emery/` carrying the `/emery:specify` skill wrapper). The live CLI grammar is three verbs:

- `emery specify <adapter>... [--description <adapter>=<text>] [--config [<path>]]` — the spec generator: resolve the sources named on the invocation (a project-relative local `.wasm` loads through the deployment's `omnia:plugins/loader` capability, read fresh each run; an exact package reference fetches from the source's `registry` override or the compiled-in `omnia.host` default; either way the source's optional `digest` pin is verified host-side), extract each source over the `Source` capability, derive the requirements under authority precedence, synthesise, and commit the typed specification and design as one revision, atomically swapping the current revision id. `--config` without a value explicitly selects `emery.toml`; a run naming no sources at all discovers the project-root `emery.toml` as a fallback — never merged with argv sources. The source list is per-run input, never persisted. Every run starts from its sources alone — nothing of the stored revision reaches the synthesis, and requirements number from `REQ-001` in source order — and a re-run reports the re-mine diff against the revision it displaced in the success envelope, never persisted. A stored revision under an older grammar refuses `spec-outdated`. (Amending an in-flight specification is future work, not yet in the grammar.)
- `emery show <spec|design>` — print a reviewable document of the current revision to stdout; text mode is the Markdown projection alone (front matter `emery: 2` / `revision: <id>`, then the body), and the JSON envelope carries the revision id, the projection, and the typed document as `document`.
- `emery completions <shell>` — auto-derived from the clap surface.

Deleted verbs are deleted from the grammar, not hidden — there are no compatibility aliases or deprecated stubs. The HTTP surface is deleted outright — no listener, no MCP shelves — so C3 (no unauthenticated HTTP ingress) is satisfied by absence rather than refusal. Adapter references reach the model through the completion session's tool closure (`list_docs` / `read_doc`, answered in-process from the embedded corpus), and the committed revision is reviewed over `emery show`.

## Vocabulary

- **source adapter** — input role: one WebAssembly component exporting the WIT `source-adapter` world (`extract` + `metadata`; no manifest file). `extract` takes a typed `SourceInput` (`key`, workspace-or-value) and returns an Evidence document of typed claims — the spec IR (A8/A16; the claim gate — id grammar + required per-kind extras — is one `emery_source::claims` rule, run as the SDK's `evidence()` check over every candidate the backend proposes so a miss is corrected in place, and re-run fail-closed engine-side because the engine cannot trust the guest). `extract` fails with `omnia_guest::Error` — `bad_request` when the adapter refuses its input, any other class for its own failure; the WIT `error` variant is lowered and lifted inside `emery_source::wire` alone. Survey, leads, and the target axis were deleted from the WIT contract (archived at `v1`); see [wit/emery.wit](wit/emery.wit).
- **engine** — this product: the engine guest (`emery:engine`), the surviving engine crates, and the engine side of the adapter contract.
- **capability** — an engine-side trait a provider carries (`Source`, `Model`, `Plugins`, `StateStore` / `BlobStore`).
- **contract** — the typed agreement (WIT, CLI, wire).
- **boundary** — the crossing you test or bind (entry points, host bindings).
- **plugin** (adapter vocabulary) vs **Cursor plugins** — do not confuse the adapter noun with `plugins/emery/`, the IDE distribution surface for the `/emery:specify` skill wrapper; the latter is invisible to the `emery` CLI.

Artifact authority is unchanged in spirit: when authoritative inputs are incomplete, preserve the gap as `[unknown]` rather than guessing.

## The Rust workspace

Leaf → root. Each publishing package is `emery-<crate>` on crates.io; Rust `use` paths follow the package name (`emery_source::`, `emery_adapter::`, `emery_engine::`, `emery_cli::`, …). The root package and the `component` rung package stay `publish = false`; the mock adapter is a root-package example, not a crate. Scripted capability doubles come from omnia's `omnia-test` crate (dev-dependency, native only), not a local crate. `prose` and `source` are the leaves both the engine and the adapters consume; `adapter` is the guest-only SDK over them, and no production crate depends on it. `cli` is a transport façade over `engine`: the engine depends on nothing CLI-shaped, and only the root's wasm32 arm and the root suites depend on `cli`.

```text
prose        # embedded prompt corpora — the runtime registry (Doc, find/body, the registry! macro over the generated DOCS table) with no dependencies; feature `emit` (build-dependencies only) adds the embed-time corpus walk + link check
source       # the emery:adapter/source contract, both sides — the WIT package + one wit-bindgen generation (wasm32; the export bindings the SDK wires into, and the engine guest's caller side in wire::import), the Evidence DTOs (types), the import-side Source capability (wasm32 defaults over wire::import; bare natively so tests script Source), and the deterministic claim gate (claims: the shared `is_kebab` / dotted-kebab id grammar, ClaimKind::required_extras, Evidence::findings); failures are `omnia_guest::Error` on both sides — the WIT `error` variant is lowered on export (`BadRequest` / `NotFound` → `invalid-request`, the rest → `internal`) and lifted in wire::import (`invalid-request` → `bad_request`, `io` / `internal` → `bad_gateway`, the description naming the source) and named nowhere else; the input types (SourceInput / SourceContent) carry serde so the engine's `specify` request reuses them
adapter      # the adapter SDK — the SourceAdapter operations trait (extract + docs; metadata defaults to the SDK's own version as the `emery-version` pin), the source! export macro, `EvidenceTurn::{bound, prepared}` (the typed user-turn envelope: source identity, bound or adapter-prepared material, reference affordance when a corpus is present, and the fixed closing ask), `evidence(model, ctx, input, system, turn)` (the one model call an adapter makes: an `omnia_guest::model::Question<Evidence>` — the derived Evidence schema as the steering hint, the claim gate as the `check` the backend corrects against, the reference tools and workspace lend following the Context), Context, the reference tool closure (list_docs / read_doc over the embedded docs, boxed by `references::answering` for the question); re-exports the source contract, omnia's model types, and omnia's `Error` with the `bad_request!` family so an adapter depends on one crate
engine       # the transport-neutral spec generator — the `specify` and `show` handler fns over the `SpecifyInput { sources }` and `ShowInput { document }` serde inputs (`ShowOutput { revision, body, document }` carries the typed document) (omnia `Handler<P, I>` fns bound at the call site, returning `Serialize` `<Verb>Output` DTOs with nothing presentational — `specify` builds `SpecifyOutput` from the committed revision id and its re-mine `Diff`), the source DTO + its rules (emery_engine::specify — SourceConfig over the contract's SourceContent and the private validate: non-empty, kebab-case unique keys, digest/registry gating, preopen-relative roots; the `adapter` field is the typed AdapterRef, parsed at the DTO boundary), the extraction leg beside the DTO (`SpecifyInput::extract`: load + the contract's claim gate re-run over the wire as `bad_request`; an adapter's own failure arrives already classified) over the provider's Source capability, then the requirement bases (`basis.rs`: the grouping judgment over the byte-equal-id baseline, authority-ranked into one `Basis` per group — subject, status, coverage, agreeing classes — numbered from `REQ-001` in the order of each group's earliest claim; `Basis::requirement` places the engine's facts: id, subject, status, coverage, sources, the winning statement as the body, the losers) and the revision (the `SpecAnswer` / `DesignAnswer` judgments checked against the bases and the claim-derived section plan, then placed beside the engine's facts in one typed `Revision`) — each judgment an `omnia_guest::model::Question<T>` put as a `Brief` — the private trait in `specify/brief.rs` one struct per judgment implements (`GroupingBrief`, `SpecBrief`, `DesignBrief`): its `NAME` the question, its `PROSE` joined as the system prompt, its `Display` the turn, its `tighten` tightening the derived schema to the run, its `verify` the acceptance gate — noting every finding on the `Review` beside the trait, the one accumulator that bullets each finding for the correction turn and whose verdict `judge` alone takes — its `into_output` deriving the judgment's `Output` (requirement bases, a document) from the brief and the accepted answer, and the provided `judge(model)` the one `Question` build, which hands `into_output` only an answer the review accepted (`From<model::Error>` maps a model failure to `bad_gateway`, a serde mismatch or the backend's spent rounds to `bad_request`) — the private `basis`, `brief`, and `synthesis` modules under `specify` (`synthesis.rs` synthesises the `Revision` and carries the prompt's `ClaimsSection` both document briefs share; the prose checks they also share — a paragraph opening no line with one of `artifact`'s `RESERVED` openers, a one-line scenario field — sit on `Review` itself in `brief.rs`; `synthesis/spec.rs` and `synthesis/design.rs` carry one judgment each: its typed answer (`SpecAnswer` is preamble plus `{ subject, scenarios }` `Draft`s — no body; the body is the engine's — whose scenarios are the specification's own `Scenario`; `DesignAnswer` is preamble plus the design's `Section<Block>` over the draft's `Block::Text(text) | Type(key)`, the one answer type kept apart because the engine adds the `signature`), its brief, and the `into_output` that builds the typed `Spec` / `Design` from the accepted answer and the engine's facts), the typed revision (the private `artifact` module with its `spec` and `design` children — the `Document` enum naming the two files, `spec.json` and `design.json`, and the serde `Revision { spec: Spec, design: Design }` holding them, whose `read(spec, design)` checks the `EMERY` grammar stamp before the shape, whose `id()` is the SHA-256 of its canonical JSON files, and whose `render(document, id)` is the Markdown projection — the `emery:` / `revision:` front matter, then each document's `Display` — `Spec` / `Requirement` / `Cited` / `Loser` / `Scenario` / `ReqId` / `Status`, `Design` / `Section<B = Block>` / `Block::Text(text) | Type { key, signature }` / `SectionKind` — plus the heading / provenance-key vocabulary the renderer owns and the `RESERVED` line openers drawn from it that no draft may use; nothing parses Markdown back), the revision store (the private `store` module: the documents committed as canonical JSON under the content id, deserialised on read after the tamper check, and the typed re-mine `Diff` — `SpecDiff` of `{ id, subject }` entries matched by `ReqId` (positional, so a requirement whose place moved reads as a change) with `fields` on a change, `DesignDiff` of section keys — exported with `ReqId` / `SectionKind` through `emery_engine::specify`; the `CURRENT` keyvalue key and `CONTAINER` blobstore name at the crate root); plus the ported kernels: emery_engine::AdapterRef + the private `adapter` module (reference parsing, the per-run `Loader` memo over the Plugins capability for local components and registry packages, the adapter `emery-version` gate) and emery_engine::preopen_path (preopen-relative path normalization); plus the `Provider` alias bound. No clap, no toml, no terminal text, no exit codes — omnia's HTTP overlay routes over it unchanged
cli          # the emery command grammar over the engine — the clap grammar (App / SpecifyArgs / ShowArgs, help prose; `show`'s document is the engine's `Document` parsed through a `PossibleValuesParser` over its `strum` keys, no mirror enum), the source carriers (argv positionals + --description, the --config emery.toml file, project-root discovery → Vec<SourceConfig>), the per-output text render fns (text.rs), and the hint table; `run(provider, argv)` drives omnia's command façade (`omnia_guest::api::command`: parse → Command::call → Response), which owns the projector, the Failure envelope, the buffered Response, and the exit map (`omnia_guest::Error::exit_code`)
emery (root) # Omnia deployment unit under src/: wasm32 engine guest cdylib (src/lib.rs — wasm32-only: bare model provider, wasi:cli/run over emery_cli::run) + the shipped runtime (src/main.rs: one omnia::runtime! embedding $OUT_DIR/emery.cwasm under the static, CWD-rooted policy — the invocation directory mounts read-only as `.`, the wasi:keyvalue/wasi:blobstore hosts bind the revision state to the durable omnia-filesystem store (compiled-in root `.omnia/storage`), Cursor answers the model, the `link:` block declares the source seam, and the `plugin:` block the `locations:` list: the `.` path root, so local `.wasm` adapters load fresh each run, then the omnia.host registry endpoint for exact package references, read fresh (no project cache)). examples/runtime.rs is a second, hand-mirrored omnia::runtime! block whose `locations:` list is path-only. Bare names still dispatch statically declared guests
```

### Repository map

```text
src/               lib.rs: the wasm32 engine guest cdylib (bare model provider, wasi:cli/run; wasm32-only — nothing native lives here); main.rs: the shipped binary, one `omnia::runtime!` block (the static CWD-rooted deployment) whose `locations:` list is the `.` path root then the omnia.host registry
crates/            the workspace crates above
examples/          the mock greeting adapter (`adapter/` — a root-package `[[example]]` cdylib: SourceAdapter + embedded prose; `docs/` is its fixture tree) + the journey runtime (`runtime.rs`, root-package example: a second `omnia::runtime!` block mirroring main.rs with a path-only `locations:` list, for the live `specify` → `show` walk over `make source`)
wit/               the emery:adapter WIT package (source-adapter world) + README
plugins/emery/     Cursor plugin: /emery:specify skill wrapper, rules, manifest
docs/              Developer Guide (mdBook; reference + contributing + standards only)
```

### Exit codes

`omnia_guest::Error::exit_code` maps the four `Error` variants 1:1 and is the single source of truth; omnia's command façade applies it to every failure `emery_cli::run` reports, and there is no exit table in this repository.

| Code | Name           | When                                                                                                                                                                                                                           |
| ---- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 0    | `EXIT_SUCCESS` | Command succeeded.                                                                                                                                                                                                             |
| 1    | `BadRequest`   | Operator or input refusal, including an adapter refusing its input. The `error` field is `specify-source-required`, `unsupported-version`, `spec-outdated`, a loader refusal (`refused`, `already-active`), or the Omnia default `bad_request`. |
| 2    | `NotFound`     | Missing resource. The `error` field is `spec-not-generated` or the Omnia default `not_found`.                                                                                                                                  |
| 3    | `ServerError`  | Unclassified default: I/O, storage, leftover conversions. The `error` field is the Omnia default `server_error` or the loader's `internal`.                                                                                    |
| 4    | `BadGateway`   | Upstream, model, adapter, or component-acquisition failure. The `error` field is the Omnia default `bad_gateway` or the loader's `unavailable`.                                                                                |
| 64   | `USAGE_EXIT`   | Clap usage error (unknown verb or flag, missing argument), rendered by clap on stderr with no envelope. `EX_USAGE`, so exit 2 always means a `NotFound` envelope.                                                              |

Omnia default codes are snake_case (`bad_request`, `not_found`, `server_error`, `bad_gateway`). The recovery and loader discriminants stay kebab-case so skills can branch on them.

## Testing philosophy

Emery strictly enforces a **root-led integration posture** (DWN-style):

- The root `tests/` scenario suites are the default home for every CLI-reachable behavior: `tests/specify.rs` (the `specify` → `show` product arc), `tests/command.rs` (the CLI wire contract), and `tests/plugin.rs` (plugin-rule mentions vs the shipped grammar) drive the in-process command façade (`emery_cli::run`) over scripted capabilities (`tests/support/provider.rs`: omnia-test's `guest::{Scripted, ScriptedLoader, Memory, Namespaced}` and the local `Source` double behind one `Provider`, each capability impl delegating to its field; `tests/support/mod.rs` adds the response assertions) and read like usage documentation. There is no wasm rung in this repository: the engine guest is compiled for wasm32 by the root build script on every native build, `make wasm` lints the adapter SDK's export side and the mock adapter for wasm32-wasip2, and the `emery:adapter/source` seam is instantiated under the real omnia runtime by `emery-adapters`' conformance rung.
- Crate suites in `crates/<name>/tests/` survive only for independently useful library contracts (the adapter SDK, prose) or product invariants impractical to arrange through the entry points; unit tests are near-zero, reserved for genuinely CLI-unreachable branches.
- Default to deletion; do not widen public APIs to test private kernels. `make cov` (workspace-wide) is the brake; `CRATE=emery-<crate> make cov-crate` audits one leaf contract.
- One `make test` invocation (per push) runs the native suites over scripted `Model` + `Source` + `Plugins` + storage (`StateStore`/`BlobStore`), with engine state asserted through the scripted store and the envelope, never the filesystem — no test builds a component or spawns a process. omnia-test's `Scripted` fails a scenario that leaves scripted turns unconsumed or runs past its script, so every scenario scripts exactly what the run consumes: every `specify` over one source consumes a spec-draft and a design-draft turn — a re-run as much as a first run, since nothing of the stored revision reaches the synthesis — a run over two or more sources a grouping turn first, and a rejected candidate one further turn per correction (the scripted double plays the backend's `check` loop: each `check` round is recorded in `exchanges()`, and a script exhausted on a rejection is the backend's spent budget). Model answers are typed JSON drafts (`tests/specify/*-draft.json`); the stored documents are asserted byte-for-byte against the engine's canonical JSON (`tests/specify/<n>-*.json`), and `emery show` stdout against the canonical Markdown projections (`tests/specify/<n>-*.md`). The v1 eval and wasm-example rungs are archived at `v1`.

See [`docs/standards/testing.md`](docs/standards/testing.md).

## Commands

All from the repository root, driven by `make` ([`Makefile`](./Makefile) → mise). Run the full local gate before committing; do not rely on bare `cargo test` or `cargo clippy`.

```bash
make ci       # check + vet + deny
make check    # fmt + lint + wasm + test + test-docs + doc + links (make fmt writes formatting)
make links    # Developer Guide link integrity (mdbook build docs, mdbook-linkcheck2)
make test     # cargo nextest run --locked --workspace --all-features --no-tests=pass, under -Dwarnings
make lint     # clippy --workspace --all-targets --all-features -- -D warnings
make wasm     # clippy of the adapter SDK + mock adapter for wasm32-wasip2 (the engine guest is built by build.rs)
make fmt      # nightly cargo fmt --all
make source   # release-build the mock adapter component for the live journey
make runtime  # run the journey runtime, e.g. `make runtime specify --config examples/emery.toml`
make cov-crate # CRATE=emery-<crate>: coverage of one leaf contract
make sweep    # drop target/ artifacts untouched for a week (cargo-sweep); cargo never collects them itself
```

Local Cursor preview of the skill wrapper: `cursor-agent --plugin-dir plugins/emery` (see [docs/contributing/operator-plugins.md](docs/contributing/operator-plugins.md)).

## Gotchas

- **Path and package adapters load dynamically; bare names stay static.** `emery specify <adapter>` accepts a package reference (`emery:intent@1.0.0`), the first-party shorthand (`intent@1.0.0` — sugar for the `emery` namespace), a bare name, or a project-relative local `.wasm` path. A local `.wasm` loads through the deployment's `omnia:plugins/loader` capability on every run that names it — read fresh (nothing mirrors, a deleted file refuses typed). A package reference fetches from the source's `registry` override or the compiled-in `omnia.host` default on every run that names it — read fresh, no project cache — and registers under the package reference itself — no parallel `source:` id. Either load is optionally pinned by the source's `digest` (the journey host in `examples/runtime.rs` declares a path-only `locations:` list and loads the built mock component by path). Bare names still dispatch only guests declared in the deployment (`src/lib.rs`); dispatch beyond the declared set fails at dispatch. GitHub URLs are refused.
- **The model never authors a document.** Synthesis is up to three typed `Question`s — a grouping partition (two or more sources), a `SpecAnswer` (scenarios for every subject; the body is the winning statement, rendered by the engine), a `DesignAnswer` — each put as a `Brief` (`GroupingBrief`, `SpecBrief`, `DesignBrief`) that steers by the derived schema plus its per-run `tighten` and validates against engine facts (the id baseline, the requirements, the section plan) with its `verify` on every candidate the backend proposes — every finding noted on one `Review`, which `judge` alone turns into the accept-or-reject verdict — then placed in the typed revision by its `into_output` over the accepted answer (`SpecBrief` in `specify/synthesis/spec.rs`, `DesignBrief` in `specify/synthesis/design.rs`). The bounded repair loop is the backend's; the correction text is omnia's; the engine owns the briefs alone. The revision is the truth and the Markdown is a projection rendered by `Display` in `artifact/`: a change to the revision's shape re-ids every revision and must update the byte-for-byte JSON fixtures under `tests/specify/`; a change to the projection layout changes only the `.md` fixtures and no id. Nothing parses Markdown back, and nothing reads a revision into a run. Prompt changes live in `crates/engine/prose/synthesis/` and change no fixture.
- Never hand-edit `.omnia/storage` state (the revision store); never `mkdir -p .omnia/...`. Route through the CLI. The source list is per-run input — argv, or an operator-owned `emery.toml` named by `--config` (or discovered at the project root by a run naming no sources at all); the engine never writes it. `spec.md` / `design.md` are `emery show` projections the skill writes beside the code for review; neither is edited by hand — change a source and re-run.
- `make links` enforces Developer Guide link integrity — renaming docs paths requires updating links in the same change.
- Crossing a major is a hard cut: no silent compatibility aliases and no migration framework. Pre-1.0, a major bump means regenerating with a fresh `emery specify`.
- Brevity caps (identifier ≤ 25, comment density) are review-only; see [coding-standards.md](docs/standards/coding-standards.md). A test `fn` names the scenario (`gen_spec`), never the outcome (`rendered_documents_read_back`) — the `//` requirement comment carries the why. WIT doc/comment caps are the same rule.

## Related coding standards

- The external Rust baseline is the [Pragmatic Rust Guidelines](https://microsoft.github.io/rust-guidelines/guidelines/index.html); [docs/standards/](docs/standards/) carries the house deltas (overrides win).
- Cross-cutting rules: [style.md](docs/standards/style.md). Lints, comments, DTOs, module layout: [coding-standards.md](docs/standards/coding-standards.md). Operation/router shape and exit mapping: [handler-shape.md](docs/standards/handler-shape.md). Workspace layout and deployment split: [architecture.md](docs/standards/architecture.md). Skill/CLI split and the JSON envelope: [cli-contract.md](docs/standards/cli-contract.md). Docs house style: [doc-authoring.md](docs/standards/doc-authoring.md).
- Lint suppressions: refactor first; `#[expect(lint, reason = "…")]` at the smallest scope; state in the PR why a refactor was infeasible.
- When you remove a symbol, `rg <SymbolName> -- AGENTS.md docs/` and update every hit in the same PR.
- Markdown changes follow [doc-authoring.md](docs/standards/doc-authoring.md); do not hard-wrap prose solely for column width.

Run `make ci` before committing. If it cannot run, say exactly why and which checks were run instead.
